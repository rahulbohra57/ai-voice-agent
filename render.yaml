services:
  - type: web
    name: ai-voice-agent
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false        # Set manually in Render dashboard
      - key: GOOGLE_API_KEY
        sync: false        # Set manually in Render dashboard
